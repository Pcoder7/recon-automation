name: Parallel Subdomain Enumeration and Live Host Check

on:
  schedule:
    - cron: "0 10 * * *"  # Runs every day at 10:00 AM UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate_matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set_matrix.outputs.matrix }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Generate Matrix from domains.txt
        id: set_matrix
        run: |
          if [ ! -f domains.txt ]; then
            echo "domains.txt file not found!"
            exit 1
          fi
          # Create a JSON array from non-empty lines in domains.txt
          matrix=$(jq -R -s -c 'split("\n") | map(select(length > 0))' domains.txt)
          echo "Matrix: $matrix"
          echo "matrix=$matrix" >> $GITHUB_OUTPUT

  scan:
    needs: generate_matrix
    runs-on: ubuntu-latest
    strategy:
      matrix:
        domain: ${{ fromJson(needs.generate_matrix.outputs.matrix) }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'  # Adjust the version as needed
      
      - name: Update & Install Dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl git jq

      - name: Install Tools
        run: |
          # Install subfinder
          go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
          # Install httpx
          go install -v github.com/projectdiscovery/httpx/cmd/httpx@latest
          # Install assetfinder
          go install -v github.com/tomnomnom/assetfinder@latest
          # Install Amass v3.23.3
          #go install -v github.com/OWASP/Amass/v3/...@v3.23.3
          #echo "$HOME/go/bin" >> $GITHUB_PATH

      - name: Create Results Directory for ${{ matrix.domain }}
        run: mkdir -p results/${{ matrix.domain }}

      - name: Run Subdomain Enumeration for ${{ matrix.domain }}
        run: |
          echo "Scanning domain: ${{ matrix.domain }}"
          # Run subfinder
          subfinder -d "${{ matrix.domain }}" -silent -o results/${{ matrix.domain }}/subfinder.txt
          # Run assetfinder
          assetfinder "${{ matrix.domain }}" > results/${{ matrix.domain }}/assetfinder.txt
          # Run Amass
          #amass enum -d "${{ matrix.domain }}" -o results/${{ matrix.domain }}/amass.txt
          # Combine results and deduplicate
          cat results/${{ matrix.domain }}/subfinder.txt results/${{ matrix.domain }}/assetfinder.txt  | sort -u > results/${{ matrix.domain }}/combined.txt

      - name: Check Live Hosts with httpx for ${{ matrix.domain }}
        run: |
          if [ -s results/${{ matrix.domain }}/combined.txt ]; then
            cat results/${{ matrix.domain }}/combined.txt | httpx -silent -o results/${{ matrix.domain }}/live.txt
          else
            echo "No subdomains found for ${{ matrix.domain }}"
          fi

      - name: Upload Artifact for ${{ matrix.domain }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.domain }}-results
          path: results/${{ matrix.domain }}
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Organize results
        run: |
          for artifact in artifacts/*; do
            domain=$(basename "$artifact" | sed 's/^results-//')
            mkdir -p "results/$domain"
            mv "$artifact"/* "results/$domain/"
          done

      - name: Compare Results
        id: compare
        run: |
          git diff --unified=0 --no-color results | grep '^+[^+]' | sed 's/^+//' > new_subdomains.txt
          if [ -s new_subdomains.txt ]; then
            echo "has_new_subdomains=true" >> $GITHUB_OUTPUT
          else
            echo "has_new_subdomains=false" >> $GITHUB_OUTPUT
          fi
      # Optional: Commit results to the repository

          
